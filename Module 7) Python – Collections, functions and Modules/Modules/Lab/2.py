"""• Write a Python program to generate random numbers using the random module."""

import random # using import keyword we can use random module

print(random.randint(1,9))#random.randint(argument) give the random no from the givan argument 